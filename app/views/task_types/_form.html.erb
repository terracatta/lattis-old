<%= simple_form_for(@task_type) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= button "Cancel" , @task_type %>
    <%= f.button :submit, class: "button bright" %>
<% end %>
