<%= simple_form_for(@scan_format) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= button "Cancel" , @scan_format %>
    <%= f.button :submit, class: "button bright" %>
<% end %>

