<%= form_for(@scan_format) do |f| %>
  <% if @scan_format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scan_format.errors.count, "error") %> prohibited this scan_format from being saved:</h2>

      <ul>
      <% @scan_format.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
