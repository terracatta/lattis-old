<%= form_for(@brain_area) do |f| %>
  <% if @brain_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brain_area.errors.count, "error") %> prohibited this brain_area from being saved:</h2>

      <ul>
      <% @brain_area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :short_name %><br />
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :left_hemisphere %><br />
    <%= f.check_box :left_hemisphere %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
