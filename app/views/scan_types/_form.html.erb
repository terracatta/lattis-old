<%= simple_form_for(@scan_type) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= button "Cancel" , @scan_type %>
    <%= f.button :submit, class: "button bright" %>
<% end %>
