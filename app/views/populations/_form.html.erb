<%= simple_form_for(@population) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= button "Cancel" , @population %>
    <%= f.button :submit, class: "button bright" %>
<% end %>
