<%= form_for(@scan_sub_type) do |f| %>
  <% if @scan_sub_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scan_sub_type.errors.count, "error") %> prohibited this scan_sub_type from being saved:</h2>

      <ul>
      <% @scan_sub_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :scan_type_id %><br />
    <%= f.number_field :scan_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
