<%= simple_form_for(@scan_sub_type) do |f| %>
    <%= f.input :name %>
    <%= f.association :scan_type %>
    <%= f.input :description %>
    <%= button "Cancel" , @scan_sub_type %>
    <%= f.button :submit, class: "button bright" %>
<% end %>
